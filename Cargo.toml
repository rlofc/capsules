[package]
name = "capsules"
authors = ["Ithai Levi"]
description = "Simple, closed-by-default podman-based workspaces"
version = "0.3.2"
edition = "2024"
rust-version = "1.88"
repository = "https://github.com/rlofc/capsules"
license-file = "./LICENSE"
readme = "./README.md"

[dependencies]
anyhow = "1"
clap = { version = "4.5.53", features = ["cargo"] }
serde = { version = "1.0.228", default-features = false, features = ["derive"] }
toml = "0.8"

[profile.release-minified]
inherits = "release"
strip = true
opt-level= "z"
lto = true

